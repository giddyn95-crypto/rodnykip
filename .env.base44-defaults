# Base44 dev defaults — placeholders so the app boots before credentials are provided.
# Real values are delivered via /run/base44/app.env (platform secrets) and override these.

# Required for OAuth login / WebSocket; empty = login disabled (app still renders).
NEXT_PUBLIC_DERIV_APP_ID=
NEXT_PUBLIC_DERIV_ENV=production
NEXT_PUBLIC_DERIV_APP_NAME=rondykip
NEXT_PUBLIC_DERIV_REFERRAL_LINK=https://t.deriv.link?t=MN28MCHXYW2K

# Optional Google Drive integration (leave blank to disable).
GD_CLIENT_ID=
GD_APP_ID=
GD_API_KEY=
